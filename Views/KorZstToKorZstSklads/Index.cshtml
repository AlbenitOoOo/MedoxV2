@model IEnumerable<Medox.Models.KorZstToKorZstSklad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ZstToZstSkladData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PerId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ZstToZstSkladData)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PerId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ZstToZstSkladId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ZstToZstSkladId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ZstToZstSkladId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
